const timeCookieRefeshToken = 30 * 24 * 60 * 60 * 1000

const timeCookieAccessToken = 30 * 24 * 60 * 60 * 1000

const cookie = {
  timeCookieAccessToken,
  timeCookieRefeshToken
}

export default cookie
